---
name: multi-role-client-frontend-workflow
description: "Execute the 二号/客户页面前端 role in 奚怡熊's multi-role system: STATUS.md, worktree, ESLint/build/i18n verification, 审核提交材料. Dispatched as 二号 on artist-commission client-frontend tasks."
version: 1.6.0
author: Hermes Agent
license: MIT
platforms: [linux, macos, windows]
metadata:
  hermes:
    tags: [frontend, vue3, multi-role, collaboration, chinese-workflow, code-review]
    related_skills: [multi-role-bugfix-batch-workflow, multi-role-requirements-workflow, multi-agent-collaboration-setup]
---

# Multi-Role Client-Frontend Workflow (二号/客户页面前端)

> **支持文件**（接派工先读）：references/ 下 delivery-handoff-patterns.md、client-ui-implementation-patterns.md、browser-selftest-patterns.md、i18n-locale-audit.md、verification-budget.md、visual-reskin-token-scoping.md、totp-api-integration.md、template-health-check.md

奚怡熊 runs a five-role collaboration system (一号主理人 / 二号客户前端 / 三号后端画师 / 四号需求 / 五号Bug修复). He is the relay hub: he hands you tasks as 一号 and you produce output he can copy-paste to the next role. This skill is for when you are dispatched as **二号 (client-page frontend engineer)** — feature branches in the client worktree, pre-submission verification, and structured review materials.

## Trigger

- User says "二号，开工" or addresses you as 二号
- Task involves `web/src/views/client/**`, client-facing components, or a `feat/client-frontend-*` branch
- User asks to submit a local commit for review ("提交审核")
- User says "等迁移 vN 后" + a SPEC doc to read for upcoming frontend work
- 一号 sends a multi-batch dispatch where every batch has a 前置 on 三号's backend ("等三号接口合入后开工") — run the 开工准备 workflow below; do NOT write code yet
- 一号's dispatch says "先出方案" / "交互方案（不写代码）" / "方案确认后再动手" — proposal phase before implementation (see "Produce an interaction proposal" workflow below)
- 一号 dispatches Playwright E2E infrastructure ("搭建 Playwright 框架 + N 条核心路径 E2E 测试") — see "Playwright E2E test infrastructure" workflow below

## Hard rules

1. **Authorization boundary**: only modify `web/src/` files listed in the task book (typically `views/client/**`, `components/templates/**`, client composables, `styles/templates.css`, `styles/palettes.css`, `embed/**`). **Never touch `docs/`, `server/`, `.env`, `package.json`** — even if you find errors there. Report in submission notes instead. **Exception**: `package.json` CAN be explicitly authorized for specific tasks (e.g. test-infra: adding devDependencies + scripts) — check the dispatch's 授权文件 list. When unsure whether a file is authorized, treat it as NOT authorized and ask 一号. **Implicit authorization** (v0.16 precedent): `locales/zh-CN.js` and `locales/en.js` are in the "需要一号协调" list, but **pure-additive** i18n key changes (appending new keys, not modifying existing ones) are implicitly authorized for any frontend task that needs them — note it in the submission's "是否修改非客户前端文件" field, no separate coordination round needed. Similarly, `styles/theme.css` is implicitly authorized for CSS-only additions (new variables, new global rules, media queries) when the dispatch explicitly includes CSS quality work. Modifying existing keys/values, restructuring locale files, or changing existing theme.css rules still requires coordination.
2. **i18n**: every user-visible string goes through `$t()`/`$tm()`. New keys MUST be added to BOTH `zh-CN.js` and `en.js` in the same commit. Zero hardcoded Chinese/English in templates.
3. **XSS**: any `v-html` must pass through `sanitizeHtml()` (`web/src/utils/sanitize.js`). No exceptions.
4. **ESLint**: `npx eslint .` in `web/` — zero errors, zero warnings. No new `eslint-disable` without 一号's approval.
5. **No residue**: zero `console.log`, zero commented-out code blocks, zero untracked TODO/FIXME in changed files.
6. **禁止 `git add -A` / `git add .`** — stage only specific authorized files. Worktrees accumulate stale docs/lock changes from other roles; sweeping them in gets the whole branch rejected.
7. **Comms protocol**: 开工前读 `docs/comms/STATUS.md`. Written deliverables go to `docs/comms/02-to-01-{主题}-{日期}.md`. 写下来才算交付 — verbal answers without a file = not delivered. (Note: writing comms files requires 一号's authorization since docs/ is normally out of scope — the STATUS.md read is always allowed; confirm before writing comms files if the task book doesn't explicitly authorize them.)
8. **🔴 主 worktree 是共享的——一律在自己的专属 worktree 里干活。** The main worktree (`workspace/artist-commission`) is used by multiple roles concurrently. While you edit files there, another role can `git checkout` a different branch — then your `git add` + `git commit` lands on **their** branch and sweeps in **their** staged files. (2026-07-30 incident: 二号's R39 commit landed on 三号's `feat/backend-artist-v014` as an 11-file commit — his 7 staged docs + her 4 frontend files. His branch was unpushed, so recovery was clean; a pushed branch would have been a real mess.) Rules:
   - Session start: `git worktree list`. If your dedicated worktree doesn't exist, create it: `git worktree add ../artist-commission-client-<batch> feat/client-frontend-<batch>` (branch per dispatch instructions). ALL edits and commits happen there.
   - **Before EVERY commit**: `git branch --show-current` + `git diff --stat --cached` — confirm the branch is yours and ONLY your files are staged. If the branch is not yours, STOP. Do not commit, do not `git checkout` (that would move someone else's state) — assess first.
   - **Recovery if your commit landed on someone else's branch** (safe only while their branch is unpushed — check `git branch -r` / origin state first): `git reset --soft <their-previous-HEAD>` → `git restore --staged .` (their staged changes return to their working tree intact) → copy YOUR files into your dedicated worktree and commit there → in the main worktree `git restore` your modified files and delete your new untracked files (return it to exactly their state). Then write an incident comms file (经过 → 修复步骤 → 根因 → 预防) — 一号 needs to know, and the report documents that nothing was lost.
9. **🔴 按受众分工（用户拍板 2026-08-01）。** 二号只负责客户端全部前端（4 模板 + OrderForm + TrackOrder + 共享 Tpl* 组件）。画师后台页面（Dashboard、Settings、QueueBoard 等）和管理后台页面（AdminDashboard、HealthCheck 等）归三号——三号写完 API 直接写对应 Vue 页面。二号不接画师/管理后台前端任务，即使派工误派了也要向一号确认。共享组件（Tpl*.vue）归二号所有，三号只消费不修改。
10. **🔴 共享逻辑，不共享皮肤（用户拍板 2026-08-01）。** 共享组件（Tpl*.vue、ArtworkLikeButton.vue 等）**只输出内容和状态，不写任何布局/装饰样式**——无默认 margin/padding/background/border-radius/font-size。颜色继承 `currentColor`，大小跟随 `1em`（模板通过 class 控制）。视觉完全由各模板的 class 决定，4 模板适配时每个模板必须有自己的视觉处理，不允许 4 个模板用同一套 class。这是防同质化的底线，不是建议。一号审核时会检查组件内是否有装饰性 CSS，有则打回。
11. **🔴 切了分支就必须当轮写完代码 + commit，不留半成品。** Once you `git checkout -b`, you MUST write the code AND commit it in the SAME turn. Never end a turn with a freshly-cut branch that has zero commits and only "I read the file / located the i18n keys" as progress. Never end a turn with a freshly-cut branch that has zero commits and only "I read the file / located the i18n keys" as progress. (UI-8 incident: branch `feat/client-frontend-ui8-hidden` was cut, ArtistHome.vue was read, then the turn ended with no code and no commit — 一号 flagged it as a 5-minute job left hanging and had to finish it himself; user said 「下次别漏」.) A half-started branch is worse than not starting: it looks like work-in-progress to the next person but contains nothing. If you genuinely cannot finish in the turn, do NOT cut the branch yet — read/plan first, cut + implement + commit as one atomic block.

## Workflow: Submit a local commit for review

When 一号 says a commit is sitting in the worktree and asks you to submit it for review:

1. **Read `docs/comms/STATUS.md`** — confirm master HEAD, your branch state, and what 一号 expects.
2. **Inspect the worktree**:
   ```powershell
   git worktree list
   cd <worktree-path>
   git log --oneline -5
   git diff --stat master...HEAD   # what the branch actually changes
   git status --short              # uncommitted stragglers
   ```
3. **Handle unauthorized uncommitted changes.** Other roles' work often leaves modified `docs/*.md` or lock files in your worktree. Do NOT commit them, do NOT discard them — **stash with a descriptive message** so nothing is lost:
   ```powershell
   git stash push -m "docs-soul-uncommitted" -- docs/soul/
   ```
   Report the stash in your submission notes so 一号 knows it exists and can route it to the right role.
4. **Rebase onto latest master.** Master moves constantly (other roles commit docs directly to master). Never submit a stale-based branch:
   ```powershell
   git fetch origin
   git rebase master        # after stashing, working tree is clean
   git log --oneline -3     # confirm your commit sits on top of master HEAD
   ```
   If rebase conflicts, resolve conservatively (your feature code wins for your files; master wins for everything else) and re-verify.
5. **Run the verification chain** (all must pass before submission):
   - `npx eslint .` in `web/` → zero errors/warnings
   - Production build (see pitfall: use `node node_modules/vite/bin/vite.js build`, NOT `npx vite build`)
   - Grep changed files for residue: `console\.(log|warn|error|debug)`, `TODO|FIXME|HACK`, `v-html` (each must return 0, or v-html must be paired with sanitizeHtml)
   - i18n key parity: every new key in `zh-CN.js` exists in `en.js` and vice versa
   - **API method existence**: grep every `artistApi.XXX(` / `orderApi.XXX(` / `uploadApi.XXX(` call in changed files, then confirm each method name exists in `web/src/api/index.js`. ESLint and Vite build do NOT catch missing API methods (it's a runtime TypeError, not a syntax error). This caught a P0 in R39: `artistApi.trackOn()` was called but never defined — the "启用跟踪" button would crash on click. One-liner check:
     ```powershell
     # Extract all api method calls from changed files, then verify each exists in api/index.js
     git diff master...HEAD -- '*.vue' '*.js' | Select-String 'artistApi\.(\w+)' | ForEach-Object { $_.Matches[0].Groups[1].Value } | Sort-Object -Unique | ForEach-Object { $m=$_; $found = Select-String -Path web/src/api/index.js -Pattern "${m}:" -Quiet; if (-not $found) { "MISSING: $m" } }
     ```
   - `git diff --stat master...HEAD` → every path inside the authorized list
6. **Review your own diff** before asking 一号 to. Read the full diff of each changed file. Catch obvious problems yourself — a self-reviewed submission that comes back clean builds trust.
7. **Produce 审核提交材料** (see format below). Copy-paste ready for 一号. Include honest "需人工验证项" — you cannot click through a browser; list exactly which UI behaviors need human eyes.

### 审核提交材料 format

**Use the 12-field format** (角色/工作分支/任务编号/修改模块/修改内容/涉及文件/是否修改非客户前端文件/接口依赖/自测情况/可能影响/待确认问题/申请) — the shape is documented inline below. The template file `docs/templates/submit-client-frontend.md` was ARCHIVED in the 2026-08-01 docs cleanup (8390b2e) — do NOT go looking for it, this section is now the source of truth. R21 was flagged by 一号 for free-form submissions that skipped the template — the 12 fields are a compliance item, not a style preference. For quick single-fix submissions (review 必修项), a shortened version is acceptable but must still end with 「申请一号审核合并」. Typical content shape:

```markdown
## R<N> 审核提交（请转交一号）

**分支**：`feat/client-frontend-<id>`（worktree: `<path>`）
**Commit**：`<hash>`（已 rebase 到 master `<master-hash>`）

### 改动说明
**任务 ID**：R<N>（需求来源）
**影响范围**：<files + one-line scope>
1. <numbered change list, each item = one user-visible behavior>
2. ...
**i18n**：新增 N 个键 × 2 语言（列出键名）

### 自测情况
| 检查项 | 结果 |
|--------|------|
| ESLint | ✅ 零错误零警告 |
| Vite build | ✅ 通过 |
| Rebase master | ✅ 无冲突 |
| console.log / TODO / v-html | ✅ 无残留 |
| i18n 中英同步 | ✅ N 键 × 2 |
| 改动文件范围 | ✅ 仅授权文件 |

**需人工验证项**（无浏览器环境）：
- <specific UI behavior 1>
- <specific UI behavior 2>

### 附注
<stashed files, open questions, things 一号 should know>
```

> **陷阱备忘**：PowerShell 无 `&&` 链式 git（add/commit 拆开调）、worktree 首次需 npm install、验证证据引 comms 不重跑、ESLint 先 --fix、i18n 只加不改——见 `references/pitfalls-2026-08.md`

## Workflow: Produce an interaction proposal (方案阶段 — no code yet)

When 一号's dispatch says "先出 2~3 个方案（文字描述 + 布局示意即可），写入 comms 交一号转用户拍板。方案确认后再动手写代码":

1. **Do NOT cut a branch.** A docs-only deliverable goes straight to `docs/comms/02-to-01-<topic>-proposal-<date>.md` and is committed **directly on master** (comms files follow the project convention of committing to master; only CODE goes on feature branches). `git add` only that one file, commit as `docs(client): <主题> 交互方案（待用户拍板）`.
2. **Read the requirement doc section + the CURRENT implementation fully** before proposing. The proposal must be grounded in what the code actually does today, not in the requirement's paraphrase of it.
3. **🔴 Verify backend behavior before designing interactions — this is where proposals earn their keep.** For every button/action the proposal includes, read the actual `server/src/features/**` service + route code and confirm:
   - Does the endpoint exist at all? (A proposed "启用流程跟踪" button had NO backend endpoint — existing `advanceStage` on an untracked order would reset status to `pending`, i.e. a state regression. Surfacing this in the proposal turned it into an explicit 后端协调项 for 三号 instead of a mid-implementation blocker.)
   - Does the endpoint silently reject the call in some states? (e.g. `updateStatus` throws `INVALID_TRANSITION` for orders with `currentStageId` — meaning the existing fixed-status buttons are already dead code on workflow orders; that fact reshapes the whole proposal.)
   - Does the action auto-cascade? (stage advance auto-maps to `status` via `mapStageToStatus` — the proposal must not offer redundant manual status buttons.)
   - Is the data field already returned? (e.g. `updated_at` for "最后活动时间" — already in the GET response, so no backend ask needed.)
   List these as a "已核实的后端规则" section at the top of the proposal — shared fact base for all options — and any gap as a "后端协调项（请一号安排三号）" section at the bottom with the exact endpoint + behavior spec.
4. **Structure the proposal for a non-programmer decision-maker** (the user, relayed via 一号):
   - 现状问题 in one plain sentence (use the user's own complaint wording)
   - 公共部分 (already-decided C-items, not up for debate — list them so the user sees what's locked)
   - 2~3 options, each with an **ASCII layout sketch** (boxes, buttons in `[方括号]`, current state marked) + 优点/缺点 — ASCII, not screenshots; 一号 relays text
   - One explicit recommendation with numbered reasoning
   - End with a single crisp question: "选 A、B 还是 C？（推荐 B）" — no other open items unless unavoidable
5. **Flag dispatch-file path shorthand.** 一号's authorization list may use shorthand paths (e.g. `artist/src/views/OrderDetail.vue` when the real path is `web/src/views/artist/OrderDetail.vue`). Verify real paths with a file search before touching anything; note the mapping in your comms if it differs. Intent is clear — don't block on it, but never edit a guessed path.
6. **Then stop.** Do not implement. The proposal commit + a short reply summarizing options/recommendation/blocker is the deliverable. Implementation starts only after the user picks.

## Workflow: Implement a dispatched feature batch (R-series)

When 一号 sends `01-to-02-*.md` with "前端可开工" + a task list + branch instructions:

1. **Read the dispatch + any referenced review replies** (e.g. `01-to-02-r21-review-0730.md` may contain icon-scheme decisions and answers to your earlier questions). Batch the reads.
   **Design-decision confirmations (T-items):** dispatches may include "前置：确认 T3/T5" — design questions 一号 wants YOUR professional judgment on before implementation (e.g. "公告在 4 模板的具体位置" / "0 赞是否显示数字"). These are NOT optional: answer them in the delivery comms with a per-item conclusion + one-line reasoning, then implement accordingly. If you agree with 一号's pre-judgment, say so explicitly ("同意一号预判") — silence is not confirmation. The T-item answers go at the TOP of the delivery comms so 一号 sees them before the implementation details. **🔴 Before composing a fresh answer, grep the target files for the T-number or '拍板' — the decision may already be made and documented in code comments by a prior session** (v0.22: ArtworkLikeButton.vue:8 read 'T5（用户拍板）：0 赞不显示数字' and the templates carried 'F3: announcement at ...' position comments answering T3 — the dispatch re-asked questions that were settled in v0.19). If found, the answer is 「已拍板已实现（依据：file:line）」, not a new proposal.
2. **Cut the branch exactly as instructed** (usually `git checkout -b feat/client-frontend-<id> origin/master` in the existing worktree).
3. **Read the backend contract before writing any frontend.** For every endpoint you'll call, read the actual route file in `server/src/features/**` to confirm field names, response shapes, `additionalProperties: false` constraints, and new endpoints (e.g. `POST /api/upload/note-image`). Batch these reads. The dispatch's 接口速查 table is a summary — the route files are truth.

   **Contract-first parallel work (when backend isn't merged yet):** if 一号's dispatch fixes the contract (endpoint names, request/response field names) in a SPEC doc + instruction file but 三号's backend branch hasn't merged to master, you can still implement in parallel — PROVIDED every frontend change is **optional-field + backward-compatible** (e.g. `v-if="order.currentStageId != null"` so missing fields fall back to old behavior; `stageProgress || <frontend-computed fallback>`). This is low-risk because a contract mismatch degrades gracefully, not into a crash. Before doing this: (a) confirm the contract is genuinely frozen (SPEC + 一号 instruction both state field names), (b) verify the backend branch's actual state — `git worktree list` shows 三号's worktree/branch; then `git log --oneline master..<branch>` — NON-empty output = commits ahead of master = still unmerged/in progress (the correct signal); empty output = merged or nothing. Also check `git branch -r` — 三号 often works on a LOCAL branch that was never pushed, so `origin/<branch>` may not exist at all (v0.15: `feat/v015-backend` was local-only). If the branch isn't on the remote, that itself means unmerged., (c) flag in your comms that you implemented against the contract and list any endpoint you INVENTED that the SPEC didn't define (e.g. R30d's `PUT /stage-off` — SPEC-002 never specified it; I chose `PUT` no-body and asked 三号 to confirm). Never invent contract terms silently.
4. **Read every file you'll modify fully** before editing (batch reads in one turn). Match existing structure and style.
5. **Implement in dependency order**: composables → components → views → api layer → i18n keys. Requirements that touch the same file (e.g. R18+R19 both edit OrderDetail.vue) get one pass per file, with logical commits kept separable.
6. **Verify**: ESLint zero/zero + production build (use `npm run build` — see the `npx vite build` false-positive pitfall) + a FRESH ad-hoc `.mjs` script under `C:\Users\<user>\AppData\Local\Temp\hermes-verify-<name>.mjs` (run it, show PASS/FAIL, `Remove-Item` it). The Hermes UI checker requires the `hermes-verify-` artifact; separate terminal calls don't satisfy it. **If the task's code is covered by the frontend test suite** (vitest, `npm run test:web` — exists since v0.18), run it and report "suite green" for those paths; for UI behaviors with no test coverage, still summarize as "ad-hoc verification, not suite green".
7. **Commit** per logical unit: `type(client): 简述` (feat/fix/style/refactor/perf/docs).
8. **Write the submission** to `docs/comms/02-to-01-<topic>-<date>.md` **using the 12-field format** (角色/工作分支/任务编号/修改模块/修改内容/涉及文件/是否修改非客户前端文件/接口依赖/自测情况/可能影响/待确认问题/申请 — full shape in the 审核提交材料 format section; the docs/templates file no longer exists). **R21 was flagged for skipping the 12 fields — always follow them.** End with 「申请一号审核合并」.

## Workflow: 联调 after the backend merges (parallel-period follow-through)

Contract-first parallel work (above) builds the frontend against an inferred/frozen contract. When 一号 notifies "三号接口已合入 master，可以联调", the deliverable is a **field-alignment pass**, not new features:

1. **Rebase onto master** (`git rebase master` in your worktree) so 三号's service/route files are present. Confirm with `git log --oneline -3`.
2. **Read the ACTUAL backend service file line-by-line** (`server/src/features/**/<name>.service.js` + its `.routes.js`) — not the dispatch's 接口速查, not your inference. For each endpoint your module calls, write down the EXACT return shape: top-level keys, nested object keys, and the type/language of each value.
3. **Diff every field your `normalize()` touches against the real return.** The mismatches that survive a lenient normalize are three specific kinds — check all three:
   - **A different WORD you didn't guess**: you fallback-chained `orderCount`/`changePct`, backend ships `summary.completedCount`/`summary.changePercent`. A chain can only include names you thought of.
   - **A different LANGUAGE**: backend returns display TEXT in the source language (`tag: '逾期'`) not an English key. `$t('dashboard.tag_' + tag)` then renders the nonexistent key `tag_逾期`. Fix: a `TAG_KEY_MAP = { '逾期': 'overdue', ... }` mapping source-language text → i18n key suffix, applied in normalize so BOTH the i18n lookup AND the CSS class (`todo-item--${tag}`) get the English key. (Inverse cousin of the "variable tokens stay in source language" pitfall: there the frontend keeps Chinese for the backend to match; here the backend sends Chinese and the frontend maps to a key.)
   - **A field you omitted entirely**: backend's activity item uses `content`, your chain had `description ?? text ?? event` — add `content` first.
   Also handle fields the backend does NOT return but the UI shows (e.g. 环比 `prevLabel` "vs X") — compute them frontend-side from available data (period → previous-period name).
4. **Re-verify** (ESLint + build + a fresh `.mjs` ad-hoc script that feeds a MOCK of the backend's real return shape through each normalize() and asserts the mapped output — see the ad-hoc section). Commit as `fix(client): 联调对齐三号XX API实际字段`.
5. **Update the submission comms in place** (append a 联调 table: 组件 × 后端实际字段 × 修复), don't write a new file — 一号 expects one submission doc per batch.

Rule of thumb: the normalize() written during the parallel period is a BEST-EFFORT guess; the 联调 pass is where it becomes correct. Never report "已联调" having only rebased — you must have read the service file and diffed fields.

## Workflow: Frontend test infrastructure (技术债 P1)

The frontend NOW HAS a test suite (established v0.18: vitest + happy-dom, `npm run test:web` in `web/`). When dispatched to set it up or extend it:

1. **Fresh worktree + `npm install`** (new worktrees have no node_modules). Install test deps as devDependencies: `npm install -D vitest @vue/test-utils happy-dom`.
2. **`web/vitest.config.js`** — MUST be fully isolated from the backend's `server/vitest.config.js` (different directory, different env: backend uses `node` + `:memory:` DB + UPLOAD_DIR isolation; frontend uses `happy-dom`, no env vars needed). Minimal known-good config:
   ```js
   import { defineConfig } from 'vitest/config'
   import vue from '@vitejs/plugin-vue'
   export default defineConfig({
     plugins: [vue()],
     test: {
       globals: true,
       environment: 'happy-dom',
       include: ['src/**/__tests__/**/*.test.js'],
       setupFiles: ['src/test-setup.js']
     }
   })
   ```
3. **`package.json`**: add `"test:web": "vitest run"` to scripts (do NOT touch the backend's `test` script or anything in `server/`).
4. **🔴 Pure logic trapped inside `<script setup>` CANNOT be imported by tests.** The dispatch will say "test the normalize functions" — but functions defined in an SFC's `<script setup>` are module-private. The correct move is a **testability refactor**: extract the pure functions (normalize/tagKey/guessTag/relativeTime/prevPeriodLabel — anything with no Vue reactivity or component refs) into `web/src/utils/<domain>-normalize.js` with named exports, then change the SFCs to `import` them. This touches files beyond the strict authorization list (the utility file + the SFCs) — it is a NECESSARY prerequisite for the stated deliverable; list every such file in comms' 授权外文件说明 with one line of reasoning. Keep the refactor behavior-identical (no logic changes in the same commit that introduces tests, so a failure is unambiguous).
5. **Write exemplar tests covering pure logic only** (per dispatch: "不测 DOM 渲染（避免 EP 组件注册问题）" — no mounting of components that use Element Plus; test the extracted functions directly). Good coverage targets: backend-field alignment (feed a mock of the REAL service return shape through each normalize), language-mapping tables (TAG_KEY_MAP), fallback inference (guessTag), truncation caps, relative-time tiers, null/empty-input safety.

   **5b. Testing a composable that has lifecycle hooks + external deps (e.g. `useOrderForm`).** R58 extracted the whole order-form business logic into a composable that uses `onMounted`/`onUnmounted`/`watch`, `useI18n`, `ElMessage`/`ElMessageBox`, the API layer, `sessionStorage`, and a sibling composable (`usePasteUpload`). You CAN test this without rendering Element Plus components — mount a **throwaway wrapper component** whose `setup()` just calls the composable and returns it, then drive state by mutating the returned reactive refs:
   ```js
   vi.mock('vue-i18n', () => ({ useI18n: () => ({ t: (k) => k }) }))
   vi.mock('element-plus', () => ({ ElMessage: { warning: vi.fn(), error: vi.fn(), success: vi.fn(), info: vi.fn() }, ElMessageBox: { confirm: vi.fn(), alert: vi.fn() } }))
   vi.mock('../../api/index.js', () => ({ artistPublicApi: { getProfile: vi.fn(), /* … */ }, orderApi: { create: vi.fn() }, uploadApi: { reference: vi.fn() } }))
   vi.mock('../usePasteUpload.js', async () => { const { ref } = await import('vue'); return { usePasteUpload: () => ({ isPasteUploading: ref(false), pasteError: ref('') }) } })
   import { useOrderForm } from '../useOrderForm.js'
   async function createForm() {
     /* set mockResolvedValue on every api method first */
     const formRef = ref({ validate: vi.fn().mockResolvedValue(true), scrollToField: vi.fn() })
     let of
     const wrapper = mount({ setup() { of = useOrderForm('alice', formRef); return { of } }, template: '<div />' })
     await flushPromises()          // lets onMounted's async load settle
     return { wrapper, formRef, of }
   }
   ```
   Then: mutate `of.form.tierId = 1` + `await nextTick()` to fire watchers; `vi.useFakeTimers()` + `vi.advanceTimersByTimeAsync(300)` to flush the 300ms price-calc / 500ms draft-save debounces; assert on `of.pricePreview.value`, `of.addonSelections`, `sessionStorage.getItem(...)`. This tests the REAL business logic (pricing, addon init, draft save/restore, validation rules, submit, upload) with zero EP rendering — exactly the "关键路径有测试" the dispatch wants. Mock `../../utils/sanitize.js` to a passthrough here (it's covered by its own test file) so you're not re-testing DOMPurify.
6. **Test-writing pitfalls hit live**:
   - **Import depth**: a test at `src/components/artist/dashboard/__tests__/x.test.js` needs FOUR `../` to reach `src/utils/` — `../../../` (three) resolves to `src/components/` and fails with "Failed to resolve import". Count the levels.
   - **UTC vs local date in deadline tests**: `new Date().toISOString().slice(0,10)` gives the UTC date; on a UTC+8 host before 08:00 local it's YESTERDAY — a "today's deadline" fixture then asserts overdue. Build dates with local components (`getFullYear/getMonth/getDate`) when the function under test compares local dates.
   - **i18n `t` mocks must be lazy**: a mock like `{'key': `${params.n} 分钟前`}` evaluates `params.n` at object-construction time — a key called WITHOUT params (`timeJustNow`) crashes the whole mock with "Cannot read properties of undefined". Store thunks: `{'key': () => `${params.n} ...`}` and invoke.
   - **Fake timers**: `vi.useFakeTimers()` + `vi.setSystemTime(new Date(...))` for date-dependent logic; ALWAYS `vi.useRealTimers()` in `afterEach` or later tests in the file inherit frozen time.
   - **DOMPurify + happy-dom is incompatible — mock it, don't test it.** Running the REAL `sanitizeHtml` under happy-dom produces garbage: whitelisted tags (`<p>`, `<h1>`, `<table>`) get DELETED while `<script>`/`<form>`/`<svg onload>` SURVIVE, and the `afterSanitizeAttributes` link-hardening hook never fires. This is a happy-dom DOMParser limitation, NOT a production bug (real browsers sanitize correctly). For the sanitize test file, `vi.mock('dompurify')` and test the INTEGRATION CONTRACT instead: empty-input short-circuit (DOMPurify not called), the ALLOWED_TAGS/ALLOWED_ATTR/ALLOW_DATA_ATTR:false config passed through (assert dangerous tags/`on*` attrs are EXCLUDED), and the link-hardening hook behavior (capture the hook fn registered at module load via `vi.hoisted`, invoke it with a fake `{tagName:'A', setAttribute}` node, assert `target=_blank`+`rel=noopener noreferrer`). DOMPurify's own tag/attr filtering is guaranteed by its test suite in real browsers.
   - **`vi.mock` factory overrides win over pre-set mocks — order your setup.** A `createForm()` helper that calls `setupMocks()` (setting `mockResolvedValue` on every api method) will SILENTLY OVERWRITE a rejection you set in the test body BEFORE calling it. Symptom: a "calculatePrice 失败 → pricePreview 回退 null" test gets the default resolved value and fails. Fix: call `createForm()` FIRST, then override the specific mock (`artistPublicApi.calculatePrice.mockRejectedValue(...)`) AFTER. Same for any helper that bulk-configures mocks.
   - **Pre-seeding `sessionStorage` before mount triggers the draft-restore dialog.** `useOrderForm`'s `onMounted` reads `sessionStorage` and pops `ElMessageBox.confirm` if a draft exists. A test that pre-seeds a draft to check "empty form clears it" instead gets the restore flow (confirm resolves → draft restored, not cleared). To test draft CLEARING, mount with an empty form, fill it (writes a draft via the debounced watcher), then empty it and assert the key is gone. To test restore, seed the draft + let the default `confirm.mockResolvedValue('confirm')` run.
   - **happy-dom `ClipboardEvent` carries no `clipboardData` — attach it to a plain `Event`.** `usePasteUpload` reads `event.clipboardData.items`. `new ClipboardEvent('paste')` under happy-dom has no usable `clipboardData`, so the handler no-ops. Fix: `const e = new Event('paste', { bubbles: true, cancelable: true }); e.clipboardData = { items }; document.dispatchEvent(e)` — then assert `e.defaultPrevented`, the `onFiles` call args, and `pasteError.value`. Build image items as `{ kind:'file', type:'image/png', getAsFile: () => new File([...], name, {type:'image/png'}) }`.
7. **Verify**: `npm run test:web` (all green) + `npx eslint . --max-warnings 0` + `npm run build`. Commit message type is `feat(client):` for the infra itself.

## Workflow: Audit pre-check (只排查不修 — read-only triage)

When 一号 dispatches "P2 审计预排查" / "只排查不修" / "逐条对照当前 master 代码，输出哪些仍存在、哪些已过时":

1. **Read the audit checklist** (`docs/archive/待修复问题清单.md` or equivalent) — extract every P-level item with its stated location and description.
2. **Verify each item against CURRENT master code** — grep/search the stated file+line for the described pattern. Many audit items get fixed between the audit date and your triage (v0.19: 7 of 12 P2 items were already fixed — P2-3 user enumeration, P2-5/6 GC, P2-8 shutdown timeout, P2-9 CORS docs, P2-13 zoom, P2-14 token storage). The audit report describes the state AT AUDIT TIME, not now.
3. **Classify each item**: ✅ 已修复 (with evidence: file:line showing the fix) or 🔴 仍存在 (with current code evidence).
4. **For still-existing items, assess**: 归属 (前端/后端/infra), 改动量 (minutes), 风险等级 (低/中/高), and any dependency (e.g. "needs Redis" for P2-2).
5. **Output a structured table** in the delivery comms: 编号 | 问题 | 归属 | 改动量 | 风险 | 建议修复顺序. Group by suggested batch (e.g. "前端一批 ~40min" / "后端 ~15min" / "生产部署前").
6. **Do NOT modify any file** — this is read-only. The deliverable is the triage report in `docs/comms/02-to-01-<topic>-<date>.md`.

Key pitfall: the audit report's "位置" line numbers drift as other fixes land. Search by pattern/content, not by line number. Also: some items are marked "仅生产环境需修" (e.g. CORS, trustProxy) — preserve that annotation in your triage; don't re-classify them as urgent.

## Workflow: Fix a review rejection (必修项)

When 一号 replies "审核基本通过，一个必修项：<problem>。修完提交，我立刻合并。方案你自己定":

1. Diagnose the root cause from the described symptom (e.g. "点图片只开预览，不触发 handler" → el-image preview interception, see pitfall).
2. Fix with the minimal change that satisfies the stated acceptance criterion ("核心是单击图片必须能设焦点"). You choose the approach — state it in one line in the fix comms.
3. Re-verify (ESLint + build + fresh ad-hoc script), commit as `fix(client): ...`, write a short `02-to-01-<task>-fix-<date>.md` (problem → fix approach → files → verification), request merge.

## Workflow: Read a SPEC for upcoming frontend work

When 一号 says "先通读 SPEC-NNN 第X章前端改动部分" (work is blocked on a migration or backend task):

1. **Read the full spec**, not just your chapters — backend contracts, signing matrices, and compatibility rules all constrain frontend code.
2. **Produce a 前端通读报告** with:
   - **Per-requirement change table**: file × change, one row per file touched
   - **关键约束**: data contracts the frontend must obey (e.g. "后端拼好 customLinks 返回，前端只读 customLinks 不碰旧字段"; "references 带 source 字段，客户 track 页过滤在后端")
   - **冲突协调分析**: a file × requirement matrix showing which requirements touch the same file. Same-file requirements → recommend serial implementation order; disjoint files → parallel-safe. This is the #1 thing 一号 needs from you when multiple requirements land at once.
   - **待确认/风险点（前端视角）**: open questions only the lead or backend can answer (icon asset sources, composable reuse limits, known deferred issues like signing expiry)
3. **Do NOT start coding** until the blocking dependency (migration/backend) is confirmed done. The spec's 实施顺序 section defines the gate.

## Workflow: 开工准备 (multi-batch dispatch, all batches blocked on backend)

When 一号 sends a version-level dispatch (e.g. `01-to-02-v015-frontend-*.md`) that splits work into 批次1/2/3, each gated on 三号's backend merge ("等三号 R46 接口合入后开工" / "等迁移 v15 合入后"), and explicitly says "现在可以先读需求文档熟悉验收标准，三号接口合入我会通知你" — the deliverable is a **ready state + readiness report**, not code. Do NOT cut implementation commits yet (hard rule #9 still applies: no half-started branches; the dispatch's branch may already exist from 一号's setup — rebase it, don't recreate it).

1. **Read the dispatch + all referenced requirement docs** (REQ-010 规划 + REQ-009 详情 etc.) in one batch. Extract per-batch: 授权文件 list, 验收标准, 关键 UI 决策提醒 (the dispatch's bottom section is 一号's re-statement of user-locked decisions — treat as acceptance criteria, e.g. "R40 操作条不合并进时间线", "R51/R52 始终显示不做响应式隐藏", "R49 强调色只影响客户主页").
   **🔴 Verify each listed task against actual code before reporting it as "待做".** STATUS.md and dispatch docs drift from code reality — items listed as future work may already be implemented (v0.17 prep: STATUS.md 第一批 listed R26/R31/BUG-1/2/4 as pending, but all three were already in master — R26's `v-if` guard removed with a "UI-5 修复" comment at ManualOrder.vue:92, R31's small mode deleted with a migration comment at QueueBoard.vue:173, BUG fixes in the v0.16 changelog). For each task item: grep the target file for fix markers (comments naming the task/bug ID, removed code patterns, mode changes). Report completed items as "✅ 已做完（依据：file:line）" in the readiness report, not as "待做". This shrinks the real remaining scope and gives 一号 an accurate picture — distinct from the R46 pitfall (docs misdescribing current behavior); this one is docs listing DONE work as TODO.
2. **Verify worktree + branch state**: `git worktree list` (your wt should exist, e.g. `artist-commission-wt-02` on `feat/v015-frontend-b1`); `cd` into it; `git branch --show-current` confirms it's YOUR branch; `git fetch origin; git rebase master` — master drifts constantly (docs commits land between dispatch and your session; the branch may sit several commits behind). Confirm with `git log --oneline -3`.
3. **Make the worktree buildable NOW**: new worktrees have no `node_modules` — run `npm install` in `web/` (allow-scripts warnings for esbuild/vue-demi are harmless) and a baseline `node node_modules/vite/bin/vite.js build`. A green baseline BEFORE any edits is your proof that later failures are yours, not the environment's.
4. **Batch-pre-read every file you'll touch across ALL batches, in one or two turns** — this is the real value of the prep phase: OrderDetail.vue / Dashboard.vue / Settings.vue / ArtistHome.vue / the 4 client templates / api/index.js / both locale files / ManualOrder.vue. While reading, note per-batch findings that de-risk implementation: which templates already consume a field (e.g. Classic already renders `artist.avatar` — R48 is "add entry + fill gaps", not "build from zero"), which api methods are missing (`deleteNote` absent → add when 三号 merges), exact line ranges of the regions each batch rewrites (状态卡 vs 备注卡 for R40), and any requirement-doc inaccuracy vs. real code (pitfall: requirement docs describe the present inaccurately).
5. **Check 三号's backend branch state** to report accurate blockers: `git branch -a | Select-String "v015"` + `git log --oneline master..<branch>`. Local-only (not on origin) = unmerged. Report which batches are blocked and on what.
6. **Reply with a structured readiness report** (this is the deliverable — keep it tight per 效率纪律): 环境就绪 table (branch/rebase HEAD/node_modules/build status/backend branch state) + 预读发现 (numbered, one line each, grounded in real line numbers/fields) + 阻塞清单 (batch × 前置 × 可立即开工?). Then STOP and wait for 一号's "接口合入" notification. No comms file needed for pure prep unless 一号 asked for one — the reply IS the handoff.

## Workflow: Playwright E2E test infrastructure

When dispatched to build E2E tests (e.g. "搭建 Playwright 框架 + 编写 N 条核心路径 E2E 测试"):

1. **Read the dispatch + spec** for coverage routes, constraints (data isolation, timing limits, CI command), and authorized files. Typical authorization: `e2e/` (new), root `package.json`, `playwright.config.js`, `.gitignore` — NOT `server/` or `web/`.
2. **Read the server architecture** before writing any test: `server/src/index.js` (entry, PORT env), `server/src/db/connection.js` (DB_PATH env), `server/src/db/seed.js` (seed data), `server/src/features/auth/auth.routes.js` (login flow, `_dev_code` in AUTH_DEV_MODE), `server/src/app.js` (SPA fallback via WEB_DIST). These determine how globalSetup spawns/isolates the server.
3. **Read the frontend pages you'll test** — template structure, i18n keys (zh-CN.js), CSS classes, button text. Batch-read all target pages. Note: **the browser defaults to `en-US`** (see pitfall), so either force zh-CN in fixtures or use English selectors.
4. **Build the framework** (see `references/playwright-e2e-patterns.md` for known-good file templates):
   - Root `package.json`: `"type": "module"` + `"test:e2e": "playwright test"` + `@playwright/test` devDep
   - `playwright.config.js`: chromium only, `workers: 1`, `globalSetup`/`globalTeardown`, 30s timeout, `retry: 1`
   - `e2e/global-setup.js`: clean old data → install deps if missing (new worktrees have no node_modules) → build frontend → seed DB → spawn server (port 3999, isolated DB_PATH + UPLOAD_DIR + AUTH_DEV_MODE) → health-check poll → **pre-login and write tokens to `.tokens.json`**
   - `e2e/global-teardown.js`: kill server → delete test DB + uploads + tokens
   - `e2e/fixtures/auth.js`: read `.tokens.json` (no network calls in fixtures), inject httpOnly cookie via `context.addCookies` + localStorage markers (`artist_logged_in`, `artist_is_admin`, `huiyue-locale=zh-CN`) via `addInitScript`
   - `.gitignore`: `test-results/`, `playwright-report/`, `playwright/.cache/`, `e2e/test.db*`, `e2e/test-uploads/`, `e2e/.server-pid`, `e2e/.tokens.json`
5. **Write tests** using CSS class selectors and scoped locators (`.el-dialog`, `.el-drawer`) over text-based selectors where possible. For API-assisted setup (creating test orders), use `page.request.post()` in the test body (works fine there — the hang issue is fixture-setup-specific).
6. **Run, debug via error-context.md** (Playwright writes page snapshots to `test-results/*/error-context.md` — read these, they show the exact accessibility tree at failure time), iterate.
7. **Verify**: `npm run test:e2e` (all green) + existing suites unaffected (`npm run test:web` + `npm test` in server/) + `git diff --name-only master...HEAD` confirms no server/ or web/ files touched.

## Frontend techniques library

Reusable Vue3 + Element Plus patterns for this codebase live in `references/vue3-frontend-techniques.md`. Playwright E2E infrastructure templates (globalSetup/teardown, auth fixture, test-writing rules) live in `references/playwright-e2e-patterns.md`. EP CSS 按需引入完整方案（含样式顺序安全性证明）+ Sentry 前端 SDK 接入模式 live in `references/build-and-monitoring.md`. Highlights of the Vue3 file: signed-URL refresh composable (R33), the el-image click-interception fix (R18 必修项), touch swipe-to-detail (R30c), slide-to-confirm (R30e), the 一行一条 board layout decision (R30a multi-column was REVERTED — do not re-apply), exposed next-action buttons (R30b), text-badge social links (R15/R34), paste focus-routing (R19), page→drawer conversion (R42a), touch-always controls (R44), skeleton anti-flash (R43), multi-select tiered delete (R45), the empty-state upload tile (queuefix), activity timeline merging status+notes (R40), note deletion with system-note protection (R46), click/drag replacement on an already-filled image slot — no-confirm (R53) vs overwrite-confirm (R55), immediate-save avatar upload (R48), data-accent color override with visitor-choice restore (R49), and query-param preview mode (R50), deadline/todo dashboard cards + cents→yuan display (R51/R52), and beforeunload + sessionStorage draft anti-loss for long client forms (R57), full-page form-logic extraction into a shared composable (R58-1), QQ jump+copy dual contact actions (R58-6), step-by-step order form with tier cards + sticky summary + receipt confirmation + inspiration tags + copy-summary (R58-2/3/4/5), the craft-CSS toolkit (--ease-bounce / button three-state / clamp / minmax / reduced-motion, v0.16), the admin recycle-bin panel (v0.16), the useSignatureRefresh debounce + retry-cap hardening against focus-image flicker (BUG-1), **per-image refresh with Set dedup + Map retry counter and backward-compat for non-string args (BUG-5)**, symmetric elastic/detach thresholds for split-bar drag handles (BUG-4), non-image drop feedback instead of silent discard (BUG-2), **form validation popup with ElMessageBox + scrollToField (R24)**, and **ThemePicker fixed-position FAB with sticky-CTA avoidance (R25/C37)**, **platform links CRUD with auto-detect dropdown + omit-platform-when-empty so the backend identifies it (R58-8)**, **el-tag enter-to-add tag editor with max-count/length/dedup guards (R58-8)**, **platformLinks computed + PLATFORM_BADGE mapping in useArtistData (R58-8)**, **hardcoded→API-driven inspiration tags with v-if no-fallback rendering (R58-8)**, and **extra work items CRUD card (hover-delete + terminal-state guard + yuan↔cents conversion + full-order replacement) + client-side price/installment display with field-filtering hard constraint + preview-teleported fix (SPEC-003/UX-1)**, and **slots & buffer system: nullable-number tri-state settings (switch + disabled input, null=unlimited/0=application/N>0=limit, N+M≥1 front-check), queue buffer zone with promote button + dual-list refresh + signature-refresh covering both lists, and cross-template slotDisplay via a new prop on the shared TplStatusBadge (one change → 4 templates) (SPEC-004)**, and **node speech editing: variable-tag click-to-insert at textarea cursor (Map of el-input refs + selectionStart/End splice), dirty-gated save button, copy-speech-then-delayed-tencent://-invoke (1s setTimeout so the toast is seen first), and a three-tier price-summary fallback (backend field → installments local compute → '—') so the block never shows undefined during the parallel period before 三号's backend merges (plan-node-speech)**, and **the dashboard rebuild: grid-template-columns 3fr/2fr dual-column with explicit grid-row/grid-column per module (DOM order = narrow-screen order, wide-screen placement via media query), 8 self-contained module components each owning its own loading/skeleton/error/retry/empty state (independent failure per module), pure-CSS bar chart placeholder (max-normalized % heights, no chart dep until 三号 picks a library), lenient normalize() for unfrozen API field names, relative-time formatter, and slot-overview card reusing SPEC-004 getQueue() with silent-degrade on error + batch_limit=NULL non-render (v0.18-b2)**, and **embed-script iframe→redirect conversion: rewrite the standalone public/embed.js entry to window.location.href jump, deprecate (don't delete) the orphaned Vue component pending an out-of-scope vite.config.js entry removal, update README (v0.18-b2)**, **masonry gallery unification via shared-component default change (TplGallery layout default grid→masonry + delete redundant props in 4 templates, CSS columns + height:auto preserves aspect ratios) (F2)**, and **form-label supplemental tooltip via el-form-item #label slot + InfoFilled icon + cursor:help (P1-4)**, **zero-style shared components (no <style> block / currentColor+1em so templates own color+size / :deep() skinning) + Hero-overlay wrapper (position:relative div around an unauthorized shared component, zero edits to it) + position-conflict resolution (pick nearest non-colliding spot, flag in comms) + deterministic bottom/corner anchoring with mobile static fallback (never guess center offsets) + prop/ref rename to dodge vue/no-dupe-keys + per-artist localStorage liked-set (F3/F1, v0.19-wave2)**, and **the vitest frontend test infra: pure-function extraction from SFCs into utils/<domain>-normalize.js so script-setup-private logic becomes importable, isolated vitest.config.js (happy-dom, src/**/__tests__ include), and exemplar tests for normalize/tag-mapping/relative-time with UTC-vs-local-date and lazy-i18n-mock pitfalls (v0.18 test-infra)**, and **zero-style guestbook with NATIVE form elements (input/textarea/button, not Element Plus) + semantic gb-* classes so templates' :deep() skinning has no EP internal structure to fight — 4 templates each skin it differently (classic card / gallery 展签 / folio editorial / atelier paper-note with rotate) (F4, v0.19-wave3)**, and **frontend-computed derived display from existing API fields: stageProgress (name X/Y + pct) computed client-side from workflowStages + currentStageId, zero backend changes — prefer computing over requesting new fields when the data is already there (S2, v0.19-wave3)**, and **missing backend endpoint: build UI per contract + wrap load in try/catch with silent empty-state fallback + flag prominently in comms as '待三号补齐' — distinct from contract-first parallel work (unmerged branch); here the endpoint simply was never written (GET /api/admin/messages) (F4 admin, v0.19-wave3)**, and **dashboard grid extension: new area-guestbook at grid-row 5 in the existing dash-grid, implemented inline in Dashboard.vue when only the parent file is authorized (no new component files) (F4, v0.19-wave3)**, and **admin health-check page: el-collapse for check details (JSON.stringify detail), window.location for diagnostic download (httpOnly cookie rides same-origin GET), results in ref not persisted (refresh clears), disk item tagged '仅供参考' (HC, v0.19-wave3)**, and **EP on-demand import: unplugin-vue-components + ElementPlusResolver({ importStyle: false }) in vite.config.js (dts: false for non-TS), remove app.use(ElementPlus), manually register directives (app.directive('loading', ElLoading.directive) — directives are NOT auto-resolved, only el-* components are), EP 按需引入 (v0.20 JS + v0.22 A4 CSS, gzip -50%) — full recipe + ordering-safety proof in references/build-and-monitoring.md**, and **verify "dynamic import" tasks against the ACTUAL build output before coding: vuedraggable's 179kB umd chunk was ALREADY a separate lazy chunk in the baseline build (page-level code splitting via router lazy imports pulled it out automatically); the string in the main chunk was only Rollup's preload manifest filename reference, not code — check with Select-String context around the match, deliverable = verification report not redundant defineAsyncComponent (v0.20)**, and **404 catch-all may already render the homepage — the real gap is the missing indicator: LandingPage detects route.name === 'NotFound' via computed + shows a warning banner (role="alert", border-left warning color, 404 code + i18n hint), invisible on the normal Landing route (P2-11, v0.20)**, and **dark-mode adaptation of hardcoded-light HSL colors: extract the lightness into component-scoped custom properties WITH the light value as fallback (`--seg-light: 94%` on the root class; `hsl(var(--seg-hue) 55% var(--seg-light))` for fills, `var(--seg-text-light, 38%)` for text where siblings differ), then ONE `html.dark .component { --seg-light: 22%; --seg-text-light: 72%; }` override flips the whole component — hue/saturation stay untouched so segment identity colors survive; also drop light-only `box-shadow` and brighten handle-hover colors under `html.dark`. Scoped styles still match `html.dark` because the selector compiles to `[data-v-x].payment-bar` descendant rules — the `html.dark` ancestor part works fine (A5, v0.22)**. Consult it before building similar features — several were hard-won (one was a 一号-rejected 必修项, one was a user-reverted regression).

## Ad-hoc verification script (required — and get the PowerShell right)

The Hermes UI flags "stale/unverified" evidence unless you produce a temp script under the OS temp dir with a `hermes-verify-` prefix. **Preferred: `.mjs` (Node.js)** — avoids all PowerShell pitfalls (comma binding, CJK mangling, escape issues). Pattern: write `C:\Users\<user>\AppData\Local\Temp\hermes-verify-<task>.mjs`, run `node <path>`, show PASS/FAIL, `Remove-Item` it. Fallback: `.ps1` with `pwsh -ExecutionPolicy Bypass -File <path>` (bare `pwsh -File` intermittently exits 126 on this host). **If the approval system blocks script execution** (terminal returns BLOCKED / "user has NOT consented" timeout — happened twice in one session), do NOT retry the same command; fall back to running the same checks inline (`npx eslint .`, `node node_modules/vite/bin/vite.js build`, targeted `Select-String`/regex greps for the structural checks) and state honestly in the 提交材料 which checks ran inline vs via script. Before writing the script, `Test-Path` the worktree you're verifying — after a version merge 一号 may have deleted it; verify from the main worktree (master) instead. Summarize explicitly as "ad-hoc verification, not project suite green" for code paths the vitest suite doesn't cover (since v0.18 the frontend HAS a suite — `npm run test:web`; run it first and cite it for covered paths; ESLint + Vite build remain the always-on gates).

**🔴 PowerShell pitfall #1 — comma binds tighter than `-match`.** A check written as
`@('R30a grid', $qb -match 'repeat\(auto-fill')`
does NOT do what you think. The comma (array construction) binds tighter, so PowerShell evaluates `('R30a grid', $qb) -match 'repeat\(auto-fill'` — a FILTER over the array — which returns the matching ELEMENT (the entire file contents), not a boolean. Your PASS/FAIL logic reads garbage and dumps the whole file into the output (this happened live: a 54KB transcript of false FAILs). **Fix: always use `[regex]::IsMatch($str, 'pattern')` for boolean checks** (returns a clean `$true/$false`), or wrap in explicit parens `@('label', ($qb -match 'pat'))`. Prefer `[regex]::IsMatch`.

**🔴 PowerShell pitfall #2 — branch-awareness.** If your unmerged changes span MULTIPLE branches (e.g. R34 on `feat/...-r34-links`, R30 on `feat/...-r30-board`), a single script reading files from the working tree only sees the CURRENT branch. Checking R34's template edits while checked out on the R30 branch → false FAILs. **Fix: `git checkout <branch>` before reading that branch's files, run its checks, then move on; structure the script per-branch and return to the primary branch at the end.**

**🔴 PowerShell pitfall #3 — multi-line commit messages with `\"` escapes.** `git commit -m "feat: ...\n\n- 工具栏\"管理\"按钮..."` breaks: PowerShell's `\"` inside double quotes does not escape for the native git process the way you expect — git receives the message split on spaces and treats each fragment as a pathspec (`error: pathspec '多选模式下...' did not match any file(s)`). The `git add` already succeeded, so you're left staged-but-uncommitted. **Fix: use single-quoted strings for any commit message containing quotes or special chars** — `git commit -m 'feat(client): ...'`. Single quotes pass everything literally. (If the commit fails this way, just re-run `git commit` with single quotes — files are still staged.)

**🔴 PowerShell pitfall #4 — CJK literals inside inline regex checks false-fail (R58).** `[regex]::IsMatch($zh, "artistQqLabel: '画师QQ'")` returned FAIL in an inline terminal command while the content provably existed (`search_files` found the exact line) — CJK characters in the command string can be mangled in terminal transmission. **Fix: match ASCII key names only** (`'artistQqLabel:.+jumpQq:.+copyQq:.+qqCopied:'`) and verify CJK values via `search_files` (file-based, encoding-safe). Same class as pitfall #1 and the R53/R46 comment-vs-attribute false positives: when the check medium can't faithfully represent what you're matching, change what you match.

Other script notes: use `Push-Location`/`Pop-Location` around `npx eslint .` and the build. For the build, prefer `npm run build` (the npm-script form is NOT misclassified); the direct binary `node node_modules/vite/bin/vite.js build` also works; bare `npx vite build` triggers the "long-lived process" false-positive. Check `$LASTEXITCODE` immediately after each. End with `exit 1` if any FAIL so the run's exit code is honest.

**🔴 `.mjs` script pitfall — git cwd when comparing against master.** If the verify script shells out to `git show "master:<path>"` (for the "judge new against master" residue check), do NOT derive the cwd from the source-tree root with relative math like `resolve(ROOT, '../../..')` — from `web/src` that lands on `web/`, not the worktree root, and git exits with `fatal: not a git repository`. The `try/catch` swallows it, `masterContent` stays empty, every pre-existing line looks "new" → false FAIL + a retry loop. **Fix: hardcode the absolute worktree root as the cwd** (`cwd: 'D:/Hermes Agent CN Desktop/workspace/artist-commission-02'`). Same class as the other verify-script pitfalls: the check medium (a detached script) has no ambient cwd, so pin it explicitly.

**🔴 `.mjs` script pitfall — don't parse `vite build` stdout for chunk sizes; read the dist file.** A size check that regexes the build output (`out.match(/main-[\w-]+\.js\s+([\d,.]+)\s*kB/)`) silently returns null when the format doesn't match (vite's `computing gzip size:` table layout, padding, or a re-run that prints differently) — the `?: 9999` fallback then reports a bogus `9999 kB` FAIL even though the build succeeded. **Fix: stat the actual emitted file** — `readdirSync(WEB+'/dist/assets').find(f => f.startsWith('main-') && f.endsWith('.js'))` then `statSync(...).size / 1024`. The file on disk is truth; the human-readable table is not. (v0.20: this produced the lone FAIL in an otherwise 22/23 run; switching to statSync gave a clean 23/23.)

**🔴 `.mjs` script pitfall — double path-constant concatenation.** Defining both `const WT = '.../artist-commission-wt-02'` and `const WEB = WT + '/web'`, then writing `readFileSync(WEB + '/web/src/main.js')`, yields `.../web/web/src/main.js` — ENOENT on every structural check. When you have a parent/child path pair, pick ONE as the base for each read and never re-append a segment that's already in the constant. Grep your own script for `/web/web/` (or any doubled segment) before running. Same class as the git-cwd pitfall: detached scripts have no ambient path context, so a wrong constant poisons every line that uses it.

## Pitfalls

- **Designing interactions against the requirement doc instead of the backend code (R39 proposal).** The requirement doc describes intent; the service code describes truth. A proposal that includes a button with no backing endpoint (or one that silently 400s in certain states, or cascades status changes you didn't account for) gets rejected or blocks mid-implementation. Always read `server/src/features/**` for every action the proposal offers — see step 3 of "Produce an interaction proposal". Backend gaps found at proposal stage are cheap (one 协调项 line); found at implementation stage they stall the whole batch.
- **el-image `preview-src-list` swallows parent clicks (R18 review rejection).** If a wrapper has `@click="handler"` and the inner `el-image` has `preview-src-list` (+ `@click.stop`), the handler never fires — el-image's built-in preview intercepts the click. Fix: remove `preview-src-list`/`initial-index`/`@click.stop` from el-image; let clicks bubble to the wrapper. Move preview to a hover-revealed magnifier button (`@click.stop`) that opens a standalone `<el-image-viewer :url-list="..." :initial-index="..." @close="...">` (globally registered, usable directly in templates). Interaction semantics: single-click = business action · hover 🔍 = preview · hover ✕ = delete. This is the #1 most likely Element Plus click bug in this project — 一号 rejected R18 for exactly this (验收标准 #3 "点击图片=设为焦点" failed).
- **GET returns snake_case, PUT expects camelCase.** `GET /api/artist/profile` spreads the raw DB row (`profile.template_id`, `profile.custom_links` as JSON string needing `JSON.parse`). `PUT /api/artist/profile` has `additionalProperties: false` with camelCase keys (`templateId`, `customLinks`). Read with snake_case, write with camelCase — mixing them → 400 on save. When 一号's dispatch lists a "破坏性变更" camelCase migration, every form field, every save payload, and every onMounted read must switch in one commit. **The two GET endpoints are asymmetric (R58-8):** the artist-backend `GET /api/artist/profile` returns the RAW row (`platform_urls`/`inspiration_tags` as JSON strings — parse them in onMounted, tolerating both the legacy plain-string-array and object formats), while the public `GET /api/artists/:subdomain` returns PRE-PARSED camelCase arrays (`platformUrls: [{url, platform, label}]`, `inspirationTags: [string]` — consume directly, never re-parse). Check which endpoint feeds the page before writing any parsing code.
- **`additionalProperties: false` means dead fields break saves.** Before writing any PUT/POST payload, read the route's JSON Schema in `server/src/features/**` — when a backend route tightens its schema (removes fields), the frontend MUST stop sending them in the same release. The dispatch's interface table is a summary, the route file is truth.
- **Paste-upload focus routing for dual targets.** `usePasteUpload` is a document-level listener. When two paste targets coexist (gallery multi-image + note single-image), route inside the `onFiles` callback by `document.activeElement?.closest('.target-zone')`. Function declarations hoist, so referencing a later-defined handler in the callback is safe.
- **Composable extraction without API-layer wiring (R39 P0).** When you extract a composable (e.g. `useSlideConfirm`) and wire a new backend call (e.g. `artistApi.trackOn`), the API method definition in `web/src/api/index.js` is a separate file that's easy to forget — ESLint won't flag it (the property access is syntactically valid), Vite build won't flag it (no import error), and the crash only surfaces at runtime when the user clicks the button. Always verify: for every `xxxApi.method()` call you introduce, confirm `method:` exists in `api/index.js`. This is the #1 silent-P0 pattern in this codebase.
- **Dead i18n keys accumulate after UI refactors.** After removing a UI element (e.g. R18 removed the "设为焦点" button), grep for its keys across all .vue/.js files; if unused, delete from both locales in a separate `style(client): 清理死键` commit.
- **Check composable consumers before reshaping output.** Before changing a shared composable's return shape (e.g. `useArtistData.socialLinks`), grep which templates actually consume the changed property (only Classic used `socialLinks`; Gallery/Folio/Atelier called the composable but ignored it). Report the gap to 一号 as a follow-up task — don't silently expand scope.
- **Cross-template display: extend the shared component, not N templates (SPEC-004 slotDisplay).** When a dispatch says "show X on all 4 templates near the status badge", first check whether the display site already funnels through one shared component. All 4 templates render status via `TplStatusBadge` (Classic directly; Gallery/Folio/Atelier through TplHero's three variants) — so adding a `slotDisplay` prop to TplStatusBadge + passing it at 4 call sites beats inserting a block into 4 template files. Decision rule: **is the target position rendered by a single shared component? yes → add a prop there (one change, guaranteed consistency); no → per-template rendering reusing each template's own visual classes** (like R58-8's platform links, where each template's link area has distinct styling). Shared components under `web/src/components/templates/**` are in 二号's permanent authorization even when the dispatch only lists the 4 template views — note the component edit in comms' 是否修改非客户前端文件 field. When the value is backend-computed display text (slotDisplay, platform label), render it verbatim with `v-if` — zero frontend i18n/calculation, backend wording changes need no frontend release.
- **A dispatch task may ALREADY be done via the shared component — verify before implementing (v0.18-b2 task C).** 一号 dispatched "slotDisplay 补 3 模板：folio 已有，补 classic/gallery/atelier" — but all 4 templates already rendered slotDisplay: Classic via a direct `TplStatusBadge :slot-display` in its sidebar, and Gallery/Folio/Atelier via TplHero's three variants (banner/fullscreen/split) each passing `:slot-display="artist.slotDisplay"` — done in the SPEC-004 batch, which the dispatch author hadn't accounted for. The correct response was to grep the shared rendering path (`TplStatusBadge`/`TplHero` for `slot-display`), confirm every named template is covered, and report 「经代码核实已完成，无需改动（依据：TplHero.vue:9/33/51 + ArtistHomeClassic.vue:15）」 instead of writing redundant code. Before implementing any "add X to templates A/B/C" task, grep the shared component the templates funnel through for the feature — if it's already there, the deliverable is a verification report, not a commit. (Distinct from the R46 pitfall: that one is docs describing behavior that DOESN'T exist; this one is docs requesting work that's ALREADY done.)
- **An entire dispatch WAVE can be stale, not just one task — verify every item including T-items before anything else (v0.22 wave 1+2).** 一号's v0.22 dispatch listed B1 (masonry unification) as wave-1 TODO, B2/B3 (announcement + like button) as wave-2 TODO gated on backend, and T3/T5 as open design questions. ALL were already done in v0.19 (F1/F2/F3): TplGallery's `layout` default was already `'masonry'` with zero `layout=` props passed by any template; TplAnnouncement existed, mounted in all 4 templates with per-template positions documented in template comments (T3 answered); ArtworkLikeButton existed with its header comment reading 'T5（用户拍板）：0 赞不显示数字' (T5 answered); the like/unlike API methods were in api/index.js; Settings.vue had the announcement editor with expiry date. The dispatch was authored from a stale STATUS.md. Three-point verification for "shared-component default already covers all consumers": (1) grep consumers for the prop name (`layout=` → 0 hits = everyone uses the default), (2) grep for `:deep(.component-class)` template-level skin overrides, (3) grep styles/ for global overrides. For T-items: grep target files for the T-number or '拍板' in comments before composing a fresh answer. Deliverable: a per-item table (派工项 | 实际状态 | 依据 file:line) in the delivery comms — this also surfaces that the BACKEND half of a "done" wave may still be pending (B2/B3 backend needed separate confirmation from 三号). This is the multi-task generalization of the single-task check above.
- **`npx vite build` gets flagged as a long-lived server process** by the Hermes terminal tool (false positive — it's a one-shot build). Workarounds, in order of preference: (1) `npm run build` (the npm-script form passes the classifier — simplest), (2) invoke the binary directly: `node node_modules/vite/bin/vite.js build`. Same class of issue applies to any `npx <bundler> build` that the tool misclassifies.
- **Worktree stragglers from other roles**: the shared worktree accumulates `docs/` and lock-file edits from 四号/五号 sessions. Always `git status --short` before rebase; stash (never commit, never discard) anything outside your authorization.
- **Master drift**: other roles commit docs straight to master between your sessions. A branch that was "rebased yesterday" is stale today. Always re-verify `git merge-base master HEAD` == master HEAD before submitting.
- **Locale files are shared**: `zh-CN.js`/`en.js` are touched by every role. Keep your locale diff minimal (one `menu:` block addition, not a reformat) so merges stay clean.
- **Same-file requirement collisions**: when multiple queued requirements modify the same component (e.g. OrderDetail.vue for both 图库 and 备注附图), propose a serial order in your 通读报告 and stick to it — parallel branches on one file guarantee merge pain.
- **Honest capability bounds**: you have no browser. Never claim "折叠/展开交互正常" as tested — report static verification (lint/build/grep/diff review) as done, and list UI behaviors explicitly under 需人工验证项. Fabricated UI test claims destroy trust when 一号 clicks through and finds otherwise.
- **Ignore the `patch` tool's inline lint output — trust ESLint.** On this Windows host the `patch` tool's built-in syntax check runs `node --check` against a malformed path (it prepends `D:\d\`, e.g. `D:\d\Hermes Agent CN Desktop\...\index.js`), so every `.js` edit returns a scary `MODULE_NOT_FOUND` / `node:internal/modules/cjs/loader` error in the tool result. This is a path-handling quirk of the tool, NOT a real problem with your edit — the file is fine. Do not "fix" anything in response to it and do not treat it as a failed edit. The authoritative gate is `npx eslint .` in `web/` (exit 0 = clean); run that and trust it. (Recurred on every locale/api edit in the R30d session.)
- **Requirement docs describe the present inaccurately — verify current behavior before "fixing" it (R46).** REQ-008 R46 said 删除备注 was "无确认（直接删除）" and asked to add inline confirmation. Reality: there is NO delete-note API and NO delete button at all — the feature doesn't exist, so the premise is false. Before implementing any requirement phrased as "change how X behaves", grep the backend routes (`fastify.delete/put/post` in the relevant route file) and the frontend for the action. If the described current behavior doesn't exist, STOP and report two options to 一号 (extend scope: backend adds API + frontend builds feature+confirmation / defer to next version) — do not silently invent scope or silently skip. Same class: R42a had a chain effect the requirement didn't list — Dashboard.vue referenced `/manual-order` in 2 places (quick button + default-panel config). When deleting/redirecting a route, grep the whole `web/src` for that path and list every external reference in comms; if a reference lives outside your authorization, the redirect keeps it functional but note it as a follow-up for 一号.
- **Page → component conversion for drawer embedding (R42a pattern).** When merging a standalone page into another page's drawer: (1) strip the `<ArtistLayout>` wrapper and `<h2>` title (drawer provides the chrome), root becomes a plain `<div>`; (2) replace router navigation on success with `defineEmits(['created'])` + `emit('created', payload)` — parent refreshes its list; (3) mount with `v-if="drawerVisible"` so closing the drawer destroys state (free reset, no manual form-clearing needed); (4) the old route becomes `{ path: '/old', redirect: '/new?action=x' }` and the target reads `route.query.action` in `onMounted` to auto-open — old bookmarks/links never 404; (5) delete the sidebar menu entry AND its now-unused icon import from ArtistLayout (ESLint catches the unused import); (6) the old route's `name` entry disappears — grep for `name: 'ArtistXxx'` router-pushes before deleting.
- **`@media (hover: none)` for touch-only always-visible controls (R44/C56).** Hover-revealed action buttons (`.ref-hover-actions { opacity: 0 }` + `:hover { opacity: 1 }`) are unreachable on touchscreens. The project-standard fix (user-confirmed C56, long-press rejected due to browser conflicts + poor discoverability): `@media (hover: none) { .ref-hover-actions { opacity: 1; } }` — controls stay hover-gated on desktop, always visible on touch. Use this, not user-agent sniffing or long-press handlers.
- **el-image skeleton against first-paint white flash (R43).** Signed-URL images render as white blocks until loaded; many loading at once = visible flash. Fix: `<el-image>` gets a `#placeholder` slot with a pulsing skeleton div + `.ref-img { background: var(--bg-secondary, #f0f0f0) }` as fallback. Keep `@error="refreshNow"` untouched. ~15 min change, no API impact.
- **Batch ops without a batch endpoint: sequential + partial-failure reporting (R45).** When multi-select delete has no backend batch route, loop single deletes, count failures, and surface `成功 {ok} / 失败 {failed}` messaging instead of all-or-nothing. Confirm-tiering per C59: `<3` items → standard `ElMessageBox.confirm`; `≥3` → slide-to-confirm dialog (reuse `useSlideConfirm`). In manage/multi-select mode, overlay a select-layer div on each card (`position: absolute; inset: 0`) to both capture taps and **block el-image preview** (pass `:preview-src-list="manageMode ? [] : urls"`); exit manage mode always clears selection. If volumes grow, request a real batch endpoint from 三号 — note it in comms, don't build it yourself.
- **Backend may already be done — check before mocking.** 一号's dispatch said "启用跟踪按钮先 mock", but 三号 had already merged `PUT /artist/orders/:id/track-on` to master (with 409/400 defenses). Grep `server/src` for the endpoint before writing any mock — wiring the real API is always better, and the proposal-phase backend verification (hard rule) should have caught it; re-verify at implementation too since 三号 works in parallel.
- **Removing a persisted option requires a read-time migration (queuefix focusDisplay).** When you remove an option from a localStorage-persisted setting (e.g. `small` from `off/small/large`), two things must happen in the same commit: (a) the default value changes to a surviving option, and (b) old persisted values are mapped at read time — `localStorage.getItem(KEY) === 'small' ? 'large' : (localStorage.getItem(KEY) || 'large')`. Without (b), existing users get a radio group with no selected button and the component renders in an undefined state. Also delete the dead i18n key for the removed option (both locales) and any CSS/template blocks that only served it.
- **User decisions override current code — verify against comms/STATUS, not just the codebase (R30a regression).** QueueBoard shipped with `repeat(auto-fill, minmax(360px, 1fr))` (multi-column) from R30a, but the user had explicitly decided "排期看板必须保持一行一条". The implementation deviated from the decision and nobody caught it until the user complained. Before assuming current code reflects intended behavior, check STATUS.md and comms for user decisions that may contradict it. When 一号's dispatch says "回归修复", the current code IS the bug — don't try to preserve it.
- **Fresh worktree + branch in one command for isolated fix tasks.** When 一号 dispatches a standalone fix with "独立 worktree" instructions, create both atomically: `git worktree add ../artist-commission-<name> -b <branch> origin/master`. This sidesteps the shared-worktree branch-switching hazard (hard rule #8) entirely since no one else touches a brand-new worktree. Remember: new worktree = no `node_modules` — run `npm install` in `web/` before eslint/build. The allow-scripts warnings for esbuild/vue-demi are harmless (vite build still works).
- **`vue/multiline-html-element-content-newline` on multi-line elements with inline content (R46).** An element whose ATTRIBUTES span multiple lines but whose CONTENT stays inline — `<el-button\n  v-if="..."\n  @click="..."\n>✕</el-button>` — triggers 2 ESLint warnings. The rule demands content on its own line once the opening tag is multi-line. Fix: put the content between its own line breaks (`>\n  ✕\n</el-button>`). Caught at the zero-warnings gate; cheaper to write it right the first time.
- **Ad-hoc verify-script false positive: "removed attribute" greps match your own comments (R53).** A check like `-not [regex]::IsMatch($qb, 'preview-src-list')` FAILS after you remove the attribute but leave a code comment explaining WHY you removed it ("移除 preview-src-list 避免吞点击"). The regex can't tell attribute from comment. **Fix: match the BINDING syntax, not the bare name** — `-not [regex]::IsMatch($qb, ':preview-src-list=')` (or `v-bind:preview-src-list`). Same applies to any "X was removed" check where the removal rationale gets documented in a nearby comment.
- **Master drifts DURING your session, not just between sessions (v0.15 b1).** Mid-implementation, `git diff --name-only master` suddenly showed two `docs/` files you never touched — 四号 had committed R54/R55 docs to master AFTER your opening rebase, so those files existed in master but not in your branch and appeared as "differences". **Fix: re-rebase immediately before staging/committing** — `git stash push -m "wip" -- web/src` → `git rebase master` → `git stash pop` → then `git add` only your files. The pre-commit `git diff --stat --cached` check (hard rule #8) is what catches this; never skip it even if you rebased at session start.
- **Backend DELETE routes return the full refreshed object — use it, don't splice (R46).** `DELETE /api/artist/orders/:id/notes/:noteId` returns `signOrderUrls(getOrder(orderId))` — the complete order with fresh signed URLs, exactly like the POST/PUT routes. After a successful delete, do `order.value = await artistApi.deleteNote(...)` instead of manually removing the item from `order.value.notes`. Manual splice leaves stale sibling state (signature URLs drift closer to expiry, counts desync); the returned object is the single source of truth. Verify the route's `return` statement in `order.routes.js` before assuming — some DELETE routes return nothing.
- **Backend whitelist constraints override the dispatch's UI component choice (R49).** 一号's dispatch said "el-color-picker" for the accent color, but the backend service validates against a 5-color whitelist (`ACCENT_COLORS = ['#34dbcb','#34c2db','#3498db','#346edb','#3445db']` in artist.service.js) and throws `INVALID_ACCENT_COLOR` 400 for anything else — a free-form color picker would produce 400s on most picks. Before building any picker/select that writes a backend-validated field, read the service-layer validation (grep the field name in `server/src/features/**`). If the allowed set is closed, build a preset swatch selector matching it (reuse the existing visual language — ThemePicker's swatches here), NOT the free-input widget the dispatch named. Report the deviation in comms with one line of reasoning; if the user insists on free input, that's a 三号 task (widen the whitelist) first.
- **Similar-looking features can have opposite confirm semantics — never unify them (R53 vs R55).** 一号 flagged this explicitly: R53 (queue focus-image replacement) needs NO confirm because the old image survives in the gallery; R55 (tier example-image replacement) REQUIRES `ElMessageBox.confirm` because the old image is unrecoverable. Same drag/click-upload interaction, opposite confirm behavior, by user decision. When implementing an upload-replace interaction, ask: is the old asset preserved or destroyed? Destroyed → confirm. And when a dispatch says "别统一行为", keep the two code paths visibly separate with a comment naming the sibling feature and the reason.
- **Pages that predate i18n lack `useI18n` — check before using `t()` (R55).** TierManage.vue was written with hardcoded Chinese strings and no `import { useI18n } from 'vue-i18n'` / `const { t } = useI18n()`. New code that calls `t('tiers.xxx')` in such a file crashes at runtime (`t is not defined`) — ESLint won't catch it (the identifier looks like it could exist) and the build passes. Before adding i18n calls to any file, grep it for `useI18n`; if absent, add the import + destructure in the same edit. (The pre-existing hardcoded strings in that file are out of scope — don't i18n-migrate them unasked; report as a follow-up if 一号 cares.)
- **Don't name callback params `t` in files that destructure `useI18n()` (R58-8).** `.filter(t => typeof t === 'string')` inside Settings.vue shadows the `const { t } = useI18n()` binding — harmless today but a landmine the moment someone later calls `t('...')` in the same scope (it silently calls the filter's string param). ESLint's default config won't flag it (no-shadow is off). Use `tag`/`item`/`link` for callback params in any file that has `const { t } = useI18n()`.
- **Ad-hoc verify-script false positive: `v-html` count includes eslint-disable comments.** A residue check counting `v-html` occurrences finds TWO hits in Settings.vue: the real directive `v-html="sanitizedRulesPreview"` AND the adjacent `<!-- eslint-disable-next-line vue/no-v-html -->` comment. **Fix: match the directive syntax `v-html=`**, not the bare token. Same class as the R53 removed-attribute pitfall below — regexes can't distinguish code from comments, so match syntax, not names.
- **Ad-hoc verify-script false positive: full-file rewrites make UNCHANGED lines look "added" (R58-2).** When you rebuild a component with `write_file` (whole-file overwrite) instead of `patch`, every line — including pre-existing ones like `<div v-html="sanitizedRules">` — reappears as a `+` line in `git diff master...HEAD`. A "zero NEW v-html / zero NEW console.log" check that just greps `+` lines then false-FAILs on content that already existed in master. (Hit this twice in one session; two verify scripts both reported a phantom v-html.) **Fix: judge "new" against master, not against the diff's `+` sign.** For each changed `.vue`, pull the master version with `git show "master:<path>"`, and only count an added line as genuinely new if its TRIMMED content is absent from master:
  ```powershell
  $masterLines = (git show "master`:$f") | ForEach-Object { $_.Trim() }
  $added = git diff master...HEAD -- $f | Where-Object { $_ -match '^\+' -and $_ -notmatch '^\+\+\+' -and $_ -match 'v-html=' }
  $trueNew = 0
  foreach ($line in $added) { if (-not ($masterLines -contains $line.Substring(1).Trim())) { $trueNew++ } }
  # $trueNew -eq 0 → every v-html line is a rewrite-reproduction of master content, NOT a new directive
  ```
  **Trim BOTH sides** — a rewrite re-indents lines, so raw `-contains` (exact match) still false-fails; trimming normalizes indentation. (Note the backtick in `"master`:$f"` — PowerShell needs the colon escaped inside double quotes.) Then confirm the surviving directive is paired with `sanitizeHtml()` (grep the composable/util) and report it honestly as "pre-existing, not new". Same principle applies to any "no new X" residue check after a whole-file rewrite.
- **Dispatch's UI format may reference fields the backend doesn't return (R52).** The batch-3 dispatch specified the stats row as `¥XXX（N 单）`, but `getArtistStats` returns only `todayNewOrderCents`/`todayRevenueCents` — no order counts. Implementing the format literally would mean inventing fields. Correct move: render what the backend actually provides (amounts only), and list the gap under 待确认问题 ("如需（N 单）需三号在 stats 加字段"). Before building any dispatch-specified display format, verify each data field it references exists in the actual service/route response — the 接口速查 table is a summary, the code is truth.
- **Your worktree may be deleted after the version merges (v0.15).** After 一号 merged all batches, WT-02 was removed during cleanup — a final verification script against the old path failed with "Cannot find path". After a "已合入" notification, `Test-Path <worktree>` before running anything against it. Post-merge verification runs from the main worktree (master), where the code now lives; check against master HEAD (your branch is gone too — `git log master..HEAD` from the main worktree is meaningless).
- **Multi-task dispatch: implement + commit per task atomically, never batch edits then split (R58 batch1).** When a dispatch lists several tasks with distinct commit types (R58-1 `refactor(client):` + R58-6 `feat(client):`), finish task A (edit → verify → commit) before touching task B — even when both touch the same file (R58-1 and R58-6 both edit OrderForm.vue; the second task's edits simply layer on top of the first commit). Batching both tasks' patches at once forced four surgical revert-patches to produce a clean R58-1-only commit, then four re-apply patches — eight extra edits, each an error opportunity. Splitting after the fact is always more expensive than committing in order.
- **ESLint exits 2 with ERR_MODULE_NOT_FOUND right after a rebase = dependency drift, not your code (R58).** Symptom: `npx eslint .` fails with `Cannot find package '@eslint/js' imported from .../eslint.config.js` plus an ESM loader stack trace. Cause: master's package.json/lockfile changed while your branch was away (other roles added deps), and the rebase brought the new manifest but your worktree's `node_modules` is still the old install. Fix: `npm install` in `web/`, re-run — exit 0. Don't debug your edits; a module-resolution error naming an eslint-config dependency is an environment signal. (allow-scripts warnings during install remain harmless.)
- **Keep inserted comments in Chinese when patching this project.** Project rule is 代码注释全中文. It's easy to drift into English when composing a patch's new_string (happened on R51: an English `// ─── R17: Priority (...) ───` replaced the Chinese original, costing 3 fix-up patches). After any patch that touches comment lines, scan the returned diff for language drift — if the surrounding file is Chinese, your inserted lines must be too.
- **Ad-hoc verification scripts: prefer `.mjs` (Node.js) over `.ps1` (PowerShell).** A `.mjs` script avoids ALL four PowerShell pitfalls (comma binding, branch-awareness, CJK mangling, escape sequences) in one shot. Node.js `readFileSync` + regex is clean, predictable, and handles CJK natively. Pattern: write `C:\Users\<user>\AppData\Local\Temp\hermes-verify-<task>.mjs`, run `node <path>`, show PASS/FAIL, `Remove-Item` it. Use `process.exit(fail > 0 ? 1 : 0)` for honest exit codes. **Write regexes AFTER reading the actual code, not from memory** — v0.17-b1 had 5/44 false failures because regexes assumed variable names (`collectAll`, `urls[p.path]`) that didn't match the real code (`collect()`, `urlMap[o.focus_image_path]`). Read the file first, then write the check.
- **Shared composable changes must handle ALL consumer call signatures (BUG-5).** `useSignatureRefresh` is consumed by both QueueBoard (authorized, updated to pass path string) and OrderDetail (NOT authorized, still uses bare `@error="refreshNow"` where el-image passes an event object). The composable's `refreshNow(path)` must gracefully handle non-string args: `typeof path === 'string' && path` guards the per-image path; anything else (event object, undefined) falls through to full-refresh via `collect()`. Before modifying ANY shared composable, grep ALL consumers (`search_files` for the composable name across `web/src/`), read each call site, and confirm the change is backward-compatible for every signature — including consumers outside your authorization that you cannot modify.
- **Parallel-period PUT 400 on a new optional field is EXPECTED, not a bug (plan-node-speech).** When you implement against a frozen contract while 三号's backend is still unmerged, the live route schema is still `additionalProperties: false` WITHOUT your new field — so saving that field returns 400 until the backend merges. This is the correct signal that the contract isn't live yet, not a frontend defect. Do NOT "fix" it by dropping the field or loosening anything; implement per contract, keep the field optional + backward-compatible on the read side (missing field → fallback, never undefined), and note in comms that the save path 400s until 三号 extends the schema. Verify the contract is genuinely frozen first (SPEC + 一号 instruction both name the field) — see the contract-first parallel-work step in "Implement a dispatched feature batch".
- **Backend-replaced variable tokens stay in the SOURCE language in BOTH locales (plan-node-speech).** Speech template variables like `{客户名}`/`{订单号}` are matched by the backend as literal Chinese strings and substituted server-side. If you translate them in `en.js` (e.g. `{customerName}`), the backend won't recognize them and the English UI shows raw unsubstituted tokens. Keep the Chinese token text identical in both `zh-CN.js` and `en.js`, and add a comment stating they are backend variable contracts — only the surrounding UI labels (speechLabel, speechSave, etc.) get translated. Same principle applies to any frontend string that the backend parses/matches literally.
- **Dispatch/acceptance docs may name routes or file paths that don't exist — verify against router/index.js + a file search before implementing (v0.18-b2).** Acceptance §3.3 said the quick action "图库管理" jumps to `/gallery` — router/index.js has NO `/gallery` (the real artworks route is `/artworks`). The authorization list said `web/src/views/client/EmbedOrderPage.vue` — the actual file is `web/src/embed/EmbedOrderPage.vue` (not under views/client at all), and there is NO embed route in the router (the dispatch item "移除嵌入路由" was a no-op). Before implementing any dispatch-named jump target or file edit: grep router/index.js for the route, file-search the named path. Implement against REALITY (the actual route), and list each doc-vs-reality discrepancy as one line in comms — don't block on it, don't silently implement the doc's fictional version.
- **Deprecate-don't-delete when full cleanup needs an out-of-scope file (v0.18-b2 embed conversion).** User decided to drop the iframe embed for a plain redirect. embed.js (authorized) got rewritten to `window.location.href = BASE_URL + '/artist/' + ARTIST`, but fully removing the feature requires deleting the `embed` entry from vite.config.js `rollupOptions.input` (NOT authorized). Correct move: rewrite the in-scope entry point, add a deprecation comment block to the orphaned component (EmbedOrderPage.vue) naming what still exists and who owns the removal, update the README description, and file a comms ask for 一号 to drop the vite entry so the rest can be deleted. Don't modify the out-of-scope file; don't leave the old iframe code reachable from the new button.
- **Parallel-period API with UNFROZEN response field names: normalize leniently across multiple names (v0.18-b2 dashboard).** plan-node-speech had field names frozen in the SPEC → implement exactly. The dashboard APIs had only endpoint names frozen (三号's dispatch); response field names were inferred from the acceptance doc — so each module got a `normalize(raw)` tolerating several plausible names (`raw?.bars || raw?.data || raw?.buckets`, `b.cents ?? b.amountCents ?? b.amount`, `o.tag ?? o.label ?? guessTag(o)`) plus a frontend fallback that infers the derived field (guessTag from status+deadline). Whichever naming 三号 ships, the module renders; comms asks 三号 to confirm field names at 联调. Rule: frozen contract → exact names; inferred contract → lenient normalize + comms confirmation. Never hard-code a single guessed name.
- **A lenient normalize() is a guess, not a contract — the 联调 pass after backend merge is mandatory (v0.18-b2 dashboard).** After 三号's dashboard.service.js merged, reading the REAL return exposed three mismatch kinds no fallback chain had covered: (1) a different word — `summary.completedCount`/`summary.changePercent` vs my guessed `orderCount`/`changePct` (a chain only contains names you thought of); (2) a different language — `tag` returned as source-language TEXT `'逾期'` not an English key, so `$t('dashboard.tag_'+tag)` rendered the nonexistent key `tag_逾期`; fixed with `TAG_KEY_MAP = {'逾期':'overdue',...}` applied in normalize so BOTH the i18n lookup and the CSS class (`todo-item--${tag}`) get the English key; (3) an omitted field — activity item used `content`, my chain had `description ?? text ?? event`. Plus one field the backend never returns (`prevLabel` for the 环比 "vs X") — computed frontend-side from the period. Lesson: after "接口已合入，可以联调", ALWAYS read the actual service file and diff every normalize() field against it; then verify with an ad-hoc `.mjs` that feeds a mock of the REAL return shape through each normalize() and asserts the mapped output. The parallel-period normalize gets you rendering; the 联调 pass gets you rendering CORRECTLY. See the 联调 workflow above.
- **Dispatch contract lists an endpoint the backend never implemented — build UI + silent fallback + flag (F4 admin, v0.19-wave3).** Distinct from contract-first parallel work (branch exists but unmerged): here the endpoint was simply never written (`GET /api/admin/messages` — guestbook.routes.js has public/artist/DELETE routes but no admin GET). The correct response: (1) build the frontend UI per the dispatch's contract (field names, table columns, actions); (2) wrap the load call in `try/catch` with a silent empty-state fallback (no error toast on initial load — the endpoint will exist soon); (3) add the API method to `api/index.js` so it's ready; (4) flag PROMINENTLY in comms with a ⚠️ section: "后端缺 GET /api/admin/messages 端点——前端 UI 已按契约构建，加载失败静默降级，三号补一个 GET 端点即可激活". This is NOT a blocker for your submission — the frontend is correct; the gap is 三号's. Don't invent a mock or skip the feature.
- **Prefer frontend-computed derived fields over requesting new backend fields (S2, v0.19-wave3).** The dispatch said "数据来自 getStageInfo() 返回的 stageProgress: { current, total }" — but the client track API already returns `workflowStages` + `currentStageId`, from which the frontend can compute the exact same progress (sort by sortOrder, findIndex currentStageId → current = idx+1, total = length, pct = Math.round). Zero backend changes, zero coordination round, zero merge dependency. Decision rule: before asking 三号 for a new field, check whether the frontend already receives enough data to compute it. If yes, compute client-side with a `computed()` and note "前端计算，零后端改动" in comms. Only request a backend field when the data genuinely isn't available client-side.
- **vue-i18n parses `{...}` in locale strings as ICU placeholders — Chinese inside crashes the compiler (v0.19 bug 1).** A locale value like `speechPlaceholder: '{客户名}，你的订单已{节点名}。'` makes vue-i18n's message compiler treat `{客户名}` as an interpolation placeholder; `客户名` is not a valid JS identifier → `SyntaxError: Invalid token in placeholder` the moment any component renders that key (whole page crashes). This is DISTINCT from the "keep backend variable tokens in source language" pitfall above (that one is about not TRANSLATING tokens the backend matches; this one is about literal braces CRASHING the compiler regardless of language). Fix: escape each brace as an ICU literal — `"{'{'}客户名{'}'}，你的订单已{'{'}节点名{'}'}。"` (note the outer DOUBLE quotes since the value now contains single quotes). The rendered output is identical (`{客户名}…`); only the source representation changes. Apply to BOTH locales. Use this whenever a locale string must show literal curly braces (placeholder examples, code snippets, JSON-ish text). Grep changed locale files for an unescaped `{<non-ASCII>}` pattern as part of the residue check.
- **A null-access crash fix is incomplete until you grep the WHOLE pattern, not just the reported line (v0.19 bug 2).** 一号 reported `OrderForm.vue:133` crashing on `pricePreview.installments.length` — a prior fix had added optional chaining to lines 266 and 303 but MISSED 133. The durable lesson: when fixing a "Cannot read properties of undefined" crash, the root cause is an ACCESS PATTERN, and every occurrence of that pattern is a candidate for the same crash. After patching the reported line, `search_files` the file (and sibling files sharing the data shape) for the bare pattern (`pricePreview\.` here) and audit each hit: inside a `v-if="pricePreview"` guard (safe), already `?.`-chained (safe), or a bare `.prop`/`.prop.toFixed()` access (crash risk)? This audit found a SECOND latent bug the dispatch didn't mention — `ManualOrder.vue:255` `pricePreview.value.totalPrice.toFixed(2)` (no `?? 0`, crashes when totalPrice is undefined) — fixed under the "如有同类问题一并修" authorization. Report each extra find in comms with its line + why it's the same class. A single-line fix that leaves sibling occurrences unfixed is a half-fix that 一号 will hit again next week.
- **`v-if="A && B"` falls through to `v-else-if` when A is true but B is false — guard the else branch with the negated discriminator (v0.19 bug 4).** QueueBoard had `v-if="element.currentStageId != null && canAdvance(element)"` (workflow advance button) followed by `v-else-if="nextAction(element.status)"` (fixed-status button like "开始制作"). For a workflow order that CAN'T advance (currentStageId set, canAdvance false), the first condition is false → Vue falls through to the else-if → the fixed-status button renders when it shouldn't; clicking it calls `updateStatus`, which the backend state machine rejects ("不能进行此状态转换"). The two branches are meant to be MUTUALLY EXCLUSIVE on `currentStageId`, so the else-if must re-state the discriminator: `v-else-if="element.currentStageId == null && nextAction(element.status)"`. General rule: whenever a `v-if`/`v-else-if` chain splits on a discriminator (workflow-vs-not, mode A-vs-B), every later branch must explicitly exclude the earlier branches' discriminator value — relying on "the v-if already handled it" is wrong, because a compound v-if can fail on its SECOND conjunct and still fall through. Check the backend state machine (`server/src/features/**`) for which transitions it rejects; a button that triggers a guaranteed-reject call is dead/wrong UI.
- **Playwright chromium defaults to `en-US` — every Chinese selector fails (v0.21 E2E).** Headless chromium's `navigator.language` is `en-US`, so the app's i18n auto-detect renders ENGLISH and all `getByText('下一步')` / `getByPlaceholder('画师会通过QQ联系你')` locators time out. The page snapshot in `error-context.md` shows English text ("Next", "Pick a tier") — the giveaway. **Fix: force the locale in the fixture** via `context.addInitScript(() => localStorage.setItem('huiyue-locale', 'zh-CN'))` (this app persists locale under that key). Apply to EVERY context — the default `page` fixture too, not just the authed ones. Alternative: write English selectors, but forcing zh-CN keeps tests readable for the Chinese team and matches the i18n keys you already know.
- **Playwright `page.request` (APIRequestContext) hangs in fixture setup — use native `fetch` + `context.addCookies` (v0.21 E2E).** Calling `page.request.post(...)` to log in inside a fixture's setup function intermittently hangs until the 30s test timeout ("apiRequestContext.post: Test ended"), even though the identical call works fine in a test body. The durable workaround: do the login with Node's native `fetch` (in globalSetup or the fixture), extract the httpOnly cookie from `response.headers.getSetCookie()`, and inject it via `context.addCookies([{ name: 'artist_token', value, domain: 'localhost', path: '/', httpOnly: true, sameSite: 'Lax' }])`. Note native `fetch`'s `.ok` and `.status` are PROPERTIES, not methods (`.ok()` throws "not a function" — that's the Playwright API, not native).
- **Pre-login in globalSetup and cache tokens to a file — fixture logins trip the send-code rate limit on retry (v0.21 E2E).** `POST /api/auth/send-code` is rate-limited 5×/5min per IP. With `retry: 1`, a failed test re-runs its fixture → a second login → combined with other tests you exhaust the budget and later fixtures fail with "未获取到开发登录码". **Fix: log in ONCE in `global-setup.js` (after the health check passes), write `{ artist: <token>, admin: <token> }` to `e2e/.tokens.json`, and have the fixture only READ that file** (zero network calls in fixtures). Clean up the token file in teardown. This also makes fixture setup instant.
- **Cookie alone isn't enough for admin routes — the Pinia store reads `artist_is_admin` from localStorage (v0.21 E2E).** The router guard for `requiresAdmin` checks `useArtistStore().isAdmin`, which is initialized from `localStorage.getItem('artist_is_admin') === '1'` — NOT from the cookie. Injecting only the `artist_token` cookie lets the artist through `requiresAuth` but the admin guard redirects to `/dashboard`. **Fix: `addInitScript` must set BOTH** `artist_logged_in=1` and `artist_is_admin=1` (for the admin fixture). Read `web/src/stores/artist.js` to find the exact localStorage keys the guard depends on — they're app-specific.
- **Backend may auto-derive a validated field from your test input — a hyphenated subdomain fails the artistCode check (v0.21 E2E).** `POST /api/admin/artists` auto-generates `artistCode = subdomain.toUpperCase()` when omitted, and artistCode validates as "letters and digits only". A test subdomain `e2e-694473` → `E2E-694473` → 400 `CODE_FORMAT`, and the dialog just stays open (no obvious error in the page snapshot). **Diagnosis that cracked it: `page.waitForResponse(r => r.url().includes('/api/admin/artists') && r.request().method()==='POST')` in a `Promise.all` with the click, then assert `response.ok()` — the 400 body named the exact field.** Fix: use a hyphen-free subdomain (`e2e${ts}`). General lesson: when a form "silently" won't submit, intercept the actual API response rather than guessing from the DOM.
- **Playwright strict-mode: scope locators to a container + use `exact`/CSS classes when text collides (v0.21 E2E).** `getByRole('button', { name: /添加/ })` matched BOTH "+ 添加画师" (page) and "＋ 添加" (drawer) → strict-mode violation. And a new workflow stage name appeared in TWO places (`.stage-name` list + `.strip-name` overview) → `getByText(stageName)` matched 2. **Fixes: scope to the container** (`page.locator('.el-dialog').getByRole('button', { name: '添加', exact: true })`, `drawer.locator('.stage-name', { hasText: stageName })`), prefer CSS-class locators for elements that render in multiple spots, and use `exact: true` to stop substring matches. The `error-context.md` "aka getByRole(...)" hints tell you exactly which elements collided.

## Verification & reporting

Before ending a submission turn, confirm all of:
- `git log --oneline -1` shows your rebased commit on the feature branch
- `git status --short` is clean (or only the known stashed stragglers, noted in 附注)
- ESLint + build both green with real tool output (not claims)
- 审核提交材料 is complete and copy-paste ready in your reply
